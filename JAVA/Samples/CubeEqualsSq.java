package Samples;

import java.util.Scanner;

public class CubeEqualsSq {

    /*
        sum of N^3 = (sum of N)^2
    */

    static private Scanner sc = new Scanner(System.in);
    public static void main(String[] args) {
        // long n = sc.nextLong();
        // long i = 0;
        // long sum = 0;
        // long sum1 = 0;
        // while (++i <= n) {
        //     sum += Math.pow(i, 3);
        // }
        // while (i-- > 0) {
        //     sum1 += i;
        // }
        // System.out.println(sum+" "+(long)Math.pow(sum1, 2));
    }
}
